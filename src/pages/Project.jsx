import PageNav from "../components/PageNav";

function Work() {
  return (
    <div>
      <PageNav />
      <h1>
        Project Pages - You may want separate pages to showcase each project in
        more detail. These would be dynamically loaded as needed without full
        page reloads.
      </h1>
    </div>
  );
}

export default Work;
